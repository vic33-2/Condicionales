numerador = float(input("Ingresa el numerador: "))
denominador = float(input("Ingrese el denominador: "))

if denominador != 0:
    division = numerador / denominador
    print(f"Ladivision es: {division}")
else:
    print("Error: Nose puede dividir entre cero.")